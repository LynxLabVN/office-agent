name = "mcp-ledger"
description = "Record marketing posts and query performance ledger over SQLite"
command = "./target/release/mcp-ledger"
transport = "stdio"
env = { LEDGER_DB = "~/.hermes/data/ledger.db" }
scopes = ["ledger:write", "ledger:read"]

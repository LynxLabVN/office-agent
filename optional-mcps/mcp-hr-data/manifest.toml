name = "mcp-hr-data"
description = "Read and write HR records (candidates, offers, compliance) over SQLite"
command = "./target/release/mcp-hr-data"
args = []
transport = "stdio"
env = { HR_DB = "~/.hermes/data/hr.db" }
scopes = ["hr:read", "hr:write"]
